#!/usr/local/bin/swiq
# This is a hash comment
// This is a slash comment
/* This is a
   block comment */

set var i = 0;
while (i < 5) {
    log(i);
    set i = i + 1;
}

log(true);
log(false);
if (true) {
    log("booleans work");
}
