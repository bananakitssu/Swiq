#!/usr/local/bin/swiq
set var<Protected> x = 5;
try {
    set x = 10;
} catch (Error as e) {
    log(e);
}

// Should let the code continue
log(x);

set var z = 5;
set local var y = 2;
set local var<Protected> u = 10;
