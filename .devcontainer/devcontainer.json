{
  "name": "Swiq Playground",
  "image": "mcr.microsoft.com/devcontainers/cpp:1-debian-12",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools"
      ]
    }
  },

  "postCreateCommand": "mkdir -p build && cd build && cmake .. && make && sudo cp swiq /usr/local/bin/swiq && cd ../examples && chmod +x .swiq && chmod +x 2.swiq && chmod +x test_switcher.swiq && chmod +x hello.swiq && chmod +x test2.swiq && cd ../",

  "remoteUser": "vscode"
}
