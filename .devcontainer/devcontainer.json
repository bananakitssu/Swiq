{
  "name": "Swiq Playground",
  "image": "mcr.microsoft.com/devcontainers/cpp:1-debian-12",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools"
      ]
    }
  },

  "postCreateCommand": "mkdir -p build && cd build && cmake .. && make && sudo cp swiq /usr/local/bin/swiq",

  "remoteUser": "vscode"
}
